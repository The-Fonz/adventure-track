CREATE TABLE analytics_access (
  id          serial PRIMARY KEY NOT NULL,
  timestamp   timestamp,
  method      char(7),
  remote      inet,
  code        text,
  usr         text,
  host        text,
  uri         text,
  agent       text,
  referer     text,
  size        text
);
